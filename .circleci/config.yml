version: 2.1
jobs:
  deploy:
    docker:
      - image: circleci/php:latest
 
    working_directory: ~/repo
 
    steps:
      - checkout
 
      - run: 
          name: Deploy Master Branch
          command: |
            sudo apt-get update
            sudo apt-get -qq install git-ftp
            echo "Deploying project ..."
            echo $(git status)
            git ftp init --init-if-not-exist --user "shiva.aryal@webo.dev" --passwd "0TQ)av9US{VX213df" ftp://webo.dev/public_html/teamwork-slack-integration
 
workflows:
  version: 2.1
  just-deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: master